{% extends 'base.html.twig' %}

{% block title %}Hello OfficerController!{% endblock %}

{% block body %}
    <div class="container">
        <div class="row mt-3 mb-4">
            <div class="col-md-4">
                <img src="https://via.placeholder.com/600x400" alt="{{ officer.name }}" class="img-fluid">
            </div>
            <div class="col-md-8">
                <h1 class="mt-4 mt-md-0">{{ officer.name }}</h1>
                <h1 class="mt-4 mt-md-0">{{ officer.spacecraftRelated }}</h1>

                <hr>
            </div>
        </div>
    </div>
{% endblock %}