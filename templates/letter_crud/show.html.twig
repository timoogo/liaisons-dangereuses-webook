{% extends 'base.html.twig' %}

{% block title %}Letter{% endblock %}

{% block body %}
    <h1>Letter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ letter.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ letter.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_letter_crud_index') }}">back to list</a>

    <a href="{{ path('app_letter_crud_edit', {'id': letter.id}) }}">edit</a>

    {{ include('letter_crud/_delete_form.html.twig') }}
{% endblock %}
