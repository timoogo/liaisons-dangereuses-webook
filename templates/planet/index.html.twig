{% extends 'base.html.twig' %}

{% block title %}Hello OfficerController!{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mt-3 mb-4">planets</h1>
        <div class="row">
            {% for planet in planets %}
                <div class="col-md-4">
                    <div class="card mb-4">
                        <img src="https://via.placeholder.com/200x150" alt="{{ planet.name }}" class="card-img-top">
                        <div class="card-body">
                            <h5 class="card-title">{{ planet.name }}</h5>

                            <div class="d-flex justify-content-between align-item-center">
                                <a href="{{ path('planets_details', {id: planet.id}) }}" class="btn btn-dark">View details</a>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}