{% extends 'base.html.twig' %}

{% block title %}Hello PlanetController!{% endblock %}

{% block body %}
    <div class="container">
        {% block navigation %}
        <nav class="flex flex-row justify-evenly items-center">
            <div class="py-4 hover:text-purple-500"><a href="{{ path('app') }}">LOGO</a></div>
            <ul class="flex flex-row py-4 justify-evenly items-center">


                <li class="flex justify-between px-4 hover:text-purple-500 font-normal hover:font-bold">
                    <a href="{{ path('planets') }}">Planets</a>
                </li>
                <li class="flex justify-between px-4 hover:text-purple-500 font-normal hover:font-bold">
                    <a href="{{ path('galaxies') }}">Galaxies</a>
                </li>
                <li class="flex justify-between px-4 hover:text-purple-500 font-normal hover:font-bold">
                    <a href="{{ path('officers') }}">Officers</a>
                </li>
                <li class="flex justify-between px-4 hover:text-purple-500 font-normal hover:font-bold">
                    <a href="{{ path('natural_satellites') }}">Natural satellites</a>
                </li>
                <li class="flex justify-between px-4 hover:text-purple-500 font-normal hover:font-bold">
                    <a href="{{ path('artificial_satellites') }}">Artifial satellites</a>
                </li>
                {% if not app.user %}
                    <li class="text-white bg-purple-700
             hover:bg-purple-500
             focus:ring-4 focus:ring-purple-300
             font-medium rounded-full text-sm px-5 py-2.5 text-center dark:bg-purple-600 dark:hover:bg-purple-700 dark:focus:ring-purple-900">
                        <a href="{{ path('app_login')  }}">Login</a>
                    </li>
                {% else %}

                    <li class="text-white bg-purple-700
             hover:bg-purple-500
             focus:ring-4 focus:ring-purple-300
             font-medium rounded-full text-sm px-5 py-2.5 text-center dark:bg-purple-600 dark:hover:bg-purple-700 dark:focus:ring-purple-900 hover:content-['{{app.user.username}}']">
                        <a href="{{ path('app_logout') }}">logout</a>
                    </li>
                    </span>

                {% endif %}
            </ul>

        </nav>

    {% endblock %}
        <div class="row mt-3 mb-4">
            <div class="col-md-4">
                <h1 class="text-4xl text-center"> Planet {{ planet.name }}</h1>
                <img src="https://via.placeholder.com/600x400" alt="{{ planet.name }}" class="mx-auto">
            </div>
            <div class="">
                <div class="flex flex-row items-baseline justify-evenly"><span>Type of hubble sequence</span>  <h2 class="inline-block pl-4 mt-8 bg-green-100 hover:bg-green-200 text-green-800 text-sm font-medium mr-2 px-2.5 py-0.5 rounded dark:bg-green-200 dark:text-green-800 dark:hover:bg-green-300"><a href="{{ path('planets') }}">{{ planet.name }}</a></h2></div>
                <div class="flex flex-row items-baseline justify-evenly"> <span>Galaxy</span>        <h2 class="inline-block pl-4 mt-8 bg-blue-100 hover:bg-blue-200 text-blue-800 text-sm font-medium mr-2 px-2.5 py-0.5 rounded dark:bg-blue-200 dark:text-blue-800 dark:hover:bg-blue-300"><a href="{{ path('galaxies_details', {id: planet.galaxy.id}) }}">{{ planet.galaxy }}</a></h2></div>
            </div>
        </div>
    </div>
{% endblock %}